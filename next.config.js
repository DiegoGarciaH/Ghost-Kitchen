/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  api: {
    bodyParser: false,
  },
}

const config = {
  api: {
    bodyParser: false,
  },
}


module.exports = nextConfig, config
